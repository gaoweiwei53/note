## 查看历史信息
```shell
git reflog
git log
```
## 回溯到历史版本
```shell
git reset --hard 版本号
```
## 分支操作
|命令|作用|
|-|-|
|`git branch 分支名`|创建分支|
|`git branch -v`|查看分支|
|`git checkout`|切换分支|
|`git merge 分支名`|把指定分支合并到当前分支|
